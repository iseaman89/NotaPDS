<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:BAPDS.Model"
             xmlns:viewmodel="clr-namespace:BAPDS.ViewModel"
             x:DataType="viewmodel:ProjectChoiseViewModel"
             x:Class="BAPDS.View.ProjectChoisePage"
             Title="ProjectChoisePage">

    <CollectionView ItemsSource="{Binding Projects}"
                    SelectionMode="None">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:ProjectDB">
                <Grid Padding="10">
                    <Frame HeightRequest="125">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer CommandParameter="{Binding .}"
                                                  Command="{Binding Source={x:RelativeSource
                                                  AncestorType={x:Type viewmodel:ProjectChoiseViewModel}},
                                                  Path=GoToProjectCommand}"/>

                        </Frame.GestureRecognizers>
                        <Label Text="{Binding FullNumber}"/>

                    </Frame>

                </Grid>

            </DataTemplate>

        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
